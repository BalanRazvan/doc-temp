# Copy to .env and fill in from the Supabase dashboard (Connect button).
# Both values are safe in the browser: Row Level Security protects the data, not secrecy.
# Only VITE_-prefixed variables reach browser code, which keeps any other secret out of the bundle.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
